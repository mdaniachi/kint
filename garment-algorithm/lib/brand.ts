/**
 * Product identity, in one place. Renaming the tool is a single edit here —
 * the header, the browser tab and every exported filename read from it.
 */

/** Shown in the interface and the browser tab. */
export const APP_NAME = "KINT STUDIO";

/** Prefix for downloaded files: kint.png, kint-layer.png, kint.webm. */
export const FILE_SLUG = "kint";

export const APP_DESCRIPTION =
  "Upload a fashion photograph, select a garment, and apply an algorithmic treatment inside it — the rest of the image stays untouched.";
